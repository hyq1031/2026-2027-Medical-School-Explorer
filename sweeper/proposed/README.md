# Sweeper proposals

One file per school per run: `<id>-YYYY-MM-DD.md`, e.g. `curtin-2026-08-06.md`.

Each proposal is a field-by-field diff — current `uniData` value → proposed value → source URL → the exact quote from that source supporting the change — checker-verified (CONFIRMED fields only) per `EXPLORER-SWEEPER-SPEC.md` §4. These sit here awaiting the human gate (§5); nothing here has been applied to `index.html`/`medicine_explorer.html` until Kevin approves it in-session.
